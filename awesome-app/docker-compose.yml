version: '2'

services:
  app:
    build: .
    volumes:
      - ./app:/usr/src/app
    ports:
      - "3000:3000"
#     networks:
#       - backend
#     depends_on:
#       - redis
#
#   redis:
#     image: redis:alpine
#     networks:
#       - backend
#
# networks:
#   backend:
